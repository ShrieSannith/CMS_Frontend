server {
    listen 80;
    server_name _;  # The underscore means "any" domain

    # Serve static files from the build directory
    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;  # Fallback to index.html for all routes
    }
}
